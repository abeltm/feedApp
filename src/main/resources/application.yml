logging.level:
    web: DEBUG
    '[com.bptn.feedApp]': DEBUG

server:
  port: 8080

spring:
  application:
    name: feedApp

  config:
    activate:
      on-profile: default

# Banner Text
  banner:
    charset: UTF-8
    location: classpath:/banner/academy.txt

# Jackson Settings
  jackson:
    time-zone: America/Toronto
    date-format: "yyyy-MM-dd HH:mm:ss"
       
# Email Settings
  mail:
    username: abeltmariam047@gmail.com
    password: hihl vnin zodu dije
    host: smtp.gmail.com
    port: 587
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
# Common Database Settings
  sql:
    init:
      continue-on-error: false
  jpa:
    show-sql: true
    open-in-view: false
    properties:
      jakarta:
        persistence:
          sharedCache:
            mode: ENABLE_SELECTIVE
      hibernate:
        '[format_sql]': true
    hibernate:
        naming.physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        

---

# Default Profile
spring:
  config:
    activate:
      on-profile: default 
       
# Database Settings
  datasource:
    url: jdbc:postgresql://localhost:5100/feedApp
    username: postgres
    password: postgres
    hikari:
      maximum-pool-size: 2
  jpa:
    hibernate:
      ddl-auto: validate
        
